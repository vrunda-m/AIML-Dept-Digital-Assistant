# === AIML Nexus Environment ===

# Local LLM (via Ollama)
LLM_MODEL=tinyllama
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama

# Embeddings + Vector DB
EMBED_MODEL=sentence-transformers/all-MiniLM-L6-v2
CHROMA_DIR=./data/chroma

# Relational DB (SQLite for now)
DB_URL=sqlite:///./data/academic.db

# Environment flag
APP_ENV=development
